import View = require('../onejs/View');

class <%= viewName %>Base extends View {
	/// <summary>
	/// Base view class for adding view logic for the <%= viewName %> view.
	///
	/// This class is optional and can be removed if unnecessary. Remove the
	/// js-baseType attribute from the <%= viewName%>.html template's root element if you do.
	/// </summary>

}

export = <%= viewName %>Base;